[project]
name = "monumental-swe-th2"
version = "0.1.0"
description = "Monumental take home assignment"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
    "websockets>=17.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
